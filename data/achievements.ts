export type Achievement = { title: string; issuer: string; date?: string; url?: string };

// Add verified certifications, awards, or publications here.
export const achievements: Achievement[] = [];
