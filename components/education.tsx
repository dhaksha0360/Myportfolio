import { education } from "@/data/education";
import { achievements } from "@/data/achievements";
import { Reveal } from "@/components/reveal";
import { SectionHeading } from "@/components/section-heading";
export function Education() { if (!education.length) return null; return <section id="education" className="section-shell"><SectionHeading eyebrow="Education" title="Academic foundation." /><div>{education.map((item) => <Reveal key={`${item.university}-${item.degree}`}><article className="grid border-t border-border py-7 md:grid-cols-[1fr_2fr]"><p className="text-sm text-muted-foreground">{item.period}</p><div><h3 className="font-semibold">{item.degree}</h3><p className="mt-1 text-sm text-muted-foreground">{item.university}{item.faculty ? ` · ${item.faculty}` : ""}</p>{item.specialization && <p className="mt-3 text-sm">{item.specialization}</p>}</div></article></Reveal>)}</div></section>; }
export function Achievements() { if (!achievements.length) return null; return <section id="achievements" className="section-shell border-t border-border"><SectionHeading eyebrow="Recognition" title="Certifications & achievements." /><div className="grid gap-4 md:grid-cols-2">{achievements.map((item) => <article className="border border-border p-5" key={`${item.issuer}-${item.title}`}><h3 className="font-semibold">{item.title}</h3><p className="mt-2 text-sm text-muted-foreground">{item.issuer}{item.date ? ` · ${item.date}` : ""}</p></article>)}</div></section>; }
